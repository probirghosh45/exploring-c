#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main()
{

    long long int a, b,mlt;
    scanf("%lld%lld", &a, &b);
    mlt=a*b;
    printf("%lld\n", mlt);
    return 0;
}
